BLYNK_CONNECTED()
{
  Serial.println("Connected to Blynk Cloud!");
// Send current actuator conditions
  Blynk.virtualWrite(V5, digitalRead(LAMPU_PIN));
  Blynk.virtualWrite(V6, servoOpen ? 1 : 0);
  Blynk.virtualWrite(V7, buzzerState ? 1 : 0);
}
